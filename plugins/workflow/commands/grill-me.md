---
description: Challenge a planned change before implementation. One question at a time, with a recommended answer.
argument-hint: "[ticket | plan | design | free-form change]"
allowed-tools: Skill, Bash(git status:*), Bash(git branch:*), Bash(git log:*), Bash(git diff:*), Read, Grep, Glob
---

Invoke the `grill-me` skill with `$ARGUMENTS`.
